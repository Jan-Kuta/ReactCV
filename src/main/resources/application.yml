spring:
  resources:
    cache-period: 0
    chain:
      cache: false
  cache:
    type: NONE
  data:
    rest:
      base-path: /api
  datasource:
    url: jdbc:postgresql://localhost/CV
    username: kutik
    password: lezcisobe
  jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: create
